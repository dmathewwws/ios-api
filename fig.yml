web:
  build: .
  command: slc run
  volumes:
    - .:/srv/data
  ports:
    - "3000:3000"
  links:
    - mysql
mysql:
  image: mysql:latest
  ports:
    - "3306:3306"
  environment:
    MYSQL_ROOT_PASSWORD:L00pback
    MYSQL_USER:demo
    MYSQL_PASSWORD:L00pback
    MYSQL_DATABASE:demo